# Глобальный План Выполнения

Статус: в работе  
Дата старта: 2026-02-27  
Владелец: core product/dev

## Правила отметок

- `[ ]` не начато
- `[~]` в работе
- `[x]` завершено
- `[!]` блокер/риск

## Цель

Сделать систему устойчивой для длинной исследовательской работы:

- точный и предсказуемый переход к источнику;
- прозрачная оценка качества ответа;
- стабильное поведение UI на длинных документах;
- регрессионный контроль ключевых сценариев.

## Этапы

### Этап 0. Контур и трекинг

- [x] Создать единый исполняемый roadmap с отметками.
- [x] Зафиксировать приоритет первых 3 этапов.

### Этап 1. Единая модель evidence/anchors (приоритет #1)

Статус: `[x]`

- [x] Подготовить v0-спеку полей evidence (id, locator, offsets, quote, source_type).
- [x] Реализовать backend v1-поля `evidence_id/anchor_id` в citations (совместимо со старым форматом).
- [x] Добавить миграцию старых citation-полей в unified view.
- [x] Вернуть в API стабильный `anchor_id` для каждого фрагмента.

### Этап 2. Детерминированная навигация и подсветка (приоритет #2)

Статус: `[x]`

- [x] Переход к источнику по `anchor_id` (а не только по поиску строки).
- [x] Подсветка по offsets, fallback по fuzzy-поиску только как резерв.
- [x] Синхронизация перехода: клик по чату -> источник -> автоскролл.

### Этап 3. E2E регрессии критического флоу (приоритет #3)

Статус: `[x]`

- [x] Добавить e2e-сценарий: upload -> question -> citation click -> highlight visible.
- [x] Добавить e2e-сценарий: document switch -> old source cleared.
- [x] Добавить e2e-сценарий: text/document mode switch in source viewer.

### Этап 4. Декомпозиция "Надежности"

Статус: `[x]`

- [x] Разделить метрики: retrieval quality / evidence coverage / answer grounding.
- [x] Показать в UI 3 отдельные метрики вместо одного процента.

### Этап 5. Производительность длинных документов

Статус: `[x]`

- [x] Виртуализация длинного текста в source text-mode.
- [x] Локальный индекс для быстрого поиска и перехода по якорям.
- [x] Профилирование на документах 1-5 МБ текста.

### Этап 6. UX-полировка Summary/Script и статусов (приоритет #4)

Статус: `[x]`

- [x] Детализировать верхние статусы (документ/саммари/скрипт), убрать неоднозначные `Готово`.
- [x] Улучшить панель настроек скрипта: добавить текущий профиль и быстрые пресеты.
- [x] Добавить явные empty-состояния для саммари/скрипта до первой генерации.
- [x] Улучшить UX редактирования скрипта: фильтр по голосу/тексту/проблемным репликам.

### Этап 7. Финальная стабилизация viewer/loading и quality-gates

Статус: `[x]`

- [x] Убрать вечный skeleton источника при подвисшем chunk-запросе, если полный текст уже получен.
- [x] Добавить таймауты API-клиента на `chunks/fulltext`, чтобы UI выходил из вечной загрузки.
- [x] Добавить регрессионные тесты на подвисший chunk-запрос и таймауты API-клиента.
- [x] Проверить сборку и тесты после фиксов.

## Журнал прогресса

### 2026-02-27

- [x] Создан исполняемый план.
- [x] Стартован Этап 1: создана начальная спека evidence/anchor.
- [x] Backend: в payload citations добавлены `evidence_id` и `anchor_id`; `VoiceQaSource` расширен этими полями.
- [x] Уже выполненные быстрые улучшения UI (до плана): режим `Документ/Текст`, автоскролл к подсветке, фиксы высоты lite-layout, очистка старого текста при смене документа.
- [x] API `/documents/{id}/chunks/{chunk_id}` теперь возвращает `evidence_id` и `anchor_id` + дублирует `anchor_id` в `source_locator`.
- [x] Frontend: добавлена нормализация citations в unified view (`normalizeCitations`) для history / query / stream / voice.
- [x] Убрано дублирование текста цитаты в чате: оставлены только чипы источников с переходом.
- [x] Lite-режим: авто-выбор источника теперь фильтруется по текущему документу/контексту, старый документ не «залипает».
- [x] SourceViewer: добавлен `offset-first` highlight (если есть `char_start/char_end`), fallback через fuzzy-поиск; при смене citation подсветка сбрасывается.
- [x] Backend `/documents/{id}/chunks/{chunk_id}` принимает `anchor_id` и применяет его к locator (offset/page/slide fallback).
- [x] Добавлены регрессионные тесты UI: `SourceViewerPane.highlight.test.jsx` (offset highlight + mode switch), `ChatPanel.smoke` (очистка источника в lite при смене документа).
- [x] Добавлен интеграционный flow-тест: вопрос -> ответ с citation -> клик по citation -> `mark` подсветки виден в Source Viewer.
- [x] Добавлен `confidence_breakdown` в Q&A/Conv/Voice API и stream (`retrieval_quality`, `evidence_coverage`, `answer_grounding`).
- [x] UI чата/voice показывает 3 отдельные метрики вместе с общей надежностью.
- [x] Source Viewer (Pane + Modal): для больших текстов включен оконный рендер по локальному line-index (без рендера всего документа в DOM), сохранены подсветка и автоскролл к цитате.
- [x] Добавлен UI-переключатель `Показать весь текст / Показывать окно` для больших документов.
- [x] Добавлен тест `SourceViewerPane.highlight`: large full text -> windowed mode + видимая подсветка.
- [x] Выполнено профилирование алгоритма окна на 1/3/5 МБ (Node benchmark):
  - 1 МБ: index ~2.98ms, window ~0.08ms;
  - 3 МБ: index ~6.93ms, window ~0.02ms;
  - 5 МБ: index ~11.25ms, window ~0.03ms.
- [x] Backend `/documents/{id}/fulltext`: добавлены диапазонные параметры `start/end/around/max_chars/anchor_id/highlight/full`, чтобы отдавать окно текста вместо полного документа.
- [x] Frontend Source Viewer (pane + modal) переведен на window-fetch по `anchor_id/highlight`; полный текст загружается только по кнопке `Показать весь текст`.
- [x] Добавлен backend-тест `test_fulltext_endpoint_supports_window_ranges` (окно по anchor, окно по highlight, full-режим).
- [x] Включен единый `viewer-first`: Source Viewer показывается сразу после загрузки/открытия документа (без первого вопроса) в Lite и Full режимах.
- [x] Full mode: добавлен постоянный блок `Источник` в колонке Studio; клик по citation в чате открывает фрагмент в этом блоке (без модального окна по умолчанию).
- [x] Lite UX-polish: добавлены финальные состояния для панели источника (пусто/загрузка/ошибка/автоиндексация) с явным статусом в шапке.
- [x] Full UX-polish: добавлен `chat-source-toolbar` (статус источника + быстрые действия `Показать документ` / `К источнику`) и пустой state при отсутствии выбранного документа.
- [x] Full UX-polish: в Sources и Studio унифицированы статусы (`автоиндексация`, `ошибка просмотра`, `загрузка`) с цветовой индикацией.
- [x] Topbar: статусы заменены на детальные (`Документ: ...`, `Саммари: ...`, `Скрипт: ...`) с количественными показателями.
- [x] ActionBar: добавлены `settings-digest` (текущий профиль) и быстрые пресеты `Быстро/Баланс/Глубоко`.
- [x] SummaryPanel: добавлены режимы отображения `Markdown/Текст`, метрики и empty-state до генерации.
- [x] ScriptPanel: добавлены фильтры списка реплик (голос/поиск/только с замечаниями), empty-state до генерации, улучшена прокрутка длинного списка.
- [x] Фаза 2 (UX + регрессии): введён shared-контракт UI-статусов (`frontend/src/utils/uiStatus.js`) и подключён в `App`, `DocumentList`, `ChatPanel`.
- [x] Убраны неоднозначные ready-строки в статусах: для `summary/script` всегда выводится количественная деталь.
- [x] Навигация чат → источник стабилизирована: в `App` добавлен idempotent-handler открытия citation (повторный клик по тому же anchor не сбрасывает позицию).
- [x] Источник “Открыть оригинал” больше не принудительно скачивает не-PDF файлы (pane + modal открывают source URL без `download=1`).
- [x] Summary: добавлена дедупликация источников перед рендером и покрыто тестами.
- [x] Script: добавлено session-перенос фильтров по документу + явный line-operation статус `loading/done/error` в строке.
- [x] Settings/Projects: унифицированы ключевые лейблы и режимы (`Тон речи`, `Формат разговора`, `Режим генерации`, `TTS-оптимизация`).
- [x] Accessibility/Responsive: добавлен global `:focus-visible`, защита от горизонтального скролла, адаптивные брейкпоинты `<=900` и `<=1280`.
- [x] Добавлены регрессионные тесты:
  - `frontend/src/utils/__tests__/uiStatus.test.js`;
  - `frontend/src/__tests__/App.integration.test.jsx` (topbar ↔ studio + reset source при смене документа);
  - расширены `SummaryPanel.smoke` и `ScriptPanel.smoke`.
- [x] Viewer loading hotfix:
  - `SourceViewerPane/Modal` теперь показывают доступный текст/документ даже если `getDocumentChunk` ещё в полёте;
  - добавлен мягкий warning-state вместо вечного `Загрузка фрагмента...`.
- [x] API client: добавлены таймауты на запросы:
  - `getDocumentChunk` — 12s;
  - `getDocumentFullText` — 20s;
  - при таймауте возвращается ошибка `REQUEST_TIMEOUT`.
- [x] Добавлены тесты:
  - `frontend/src/api/__tests__/client.timeout.test.js`;
  - расширен `frontend/src/components/__tests__/SourceViewerPane.highlight.test.jsx` кейсом подвисшего chunk.
- [x] Повторно подтверждены quality-gates: `npm test` и `npm run build` в `frontend` зелёные.
